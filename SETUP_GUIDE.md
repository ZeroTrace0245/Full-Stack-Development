# Setup Guide

## Prerequisites
- Node.js
- npm

## Frontend
Install dependencies and start the development server.

## Backend
Move to the `backend` directory, install dependencies, configure environment variables, and start the server.

Never commit real secrets. Use `.env.example` for configuration templates.
