# Environment Variables

Keep environment-specific values outside Git. Use `.env.example` files to document required names without real credentials.

Typical categories include:
- server port
- MongoDB connection string
- JWT secret
- frontend API URL
- allowed origins
